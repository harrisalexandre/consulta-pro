import React, { useEffect, useState } from 'react'
import { Navigate, NavLink, Outlet, Route, Routes, useNavigate } from 'react-router-dom'
import { Activity, BarChart3, Bot, Building2, CalendarDays, CheckCircle2, Eye, EyeOff, LayoutDashboard, LogIn, LogOut, MessageCircle, Plus, Settings, ShieldCheck, UserRound, Users, Wifi, XCircle } from 'lucide-react'
import { supabase } from './lib/supabase'
import { TenantProvider, useTenant } from './contexts/TenantContext'
import Landing from './Landing'

function Logo(){return <div className="app-logo"><img src="/favicon.svg" alt="Consulta Pro"/><span>Consulta Pro</span></div>}
function Login(){const navigate=useNavigate();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[showPassword,setShowPassword]=useState(false);const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');if(!supabase){setError('Supabase não configurado.');setLoading(false);return}const{error}=await supabase.auth.signInWithPassword({email,password});if(error){setError('E-mail ou senha inválidos.');setLoading(false);return}navigate('/app')}return <div className="auth"><div className="auth-card"><Logo/><h1>Bem-vindo de volta</h1><p>Entre para acessar o sistema.</p><form onSubmit={submit}><label>E-mail<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Senha<div className="password-wrap"><input type={showPassword?'text':'password'} required value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" className="icon-btn" onClick={()=>setShowPassword(v=>!v)}>{showPassword?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{error&&<div className="form-error">{error}</div>}<button className="hero-btn full" disabled={loading}>{loading?'Entrando...':'Entrar'}</button></form><button className="back" onClick={()=>navigate('/')}>Voltar para o início</button></div></div>}
function SessionGate(){const[state,setState]=useState<'loading'|'ready'|'none'>('loading');useEffect(()=>{let active=true;async function check(){if(!supabase){if(active)setState('none');return}const{data}=await supabase.auth.getSession();if(active)setState(data.session?'ready':'none')}check();const sub=supabase?.auth.onAuthStateChange(()=>check()).data.subscription;return()=>{active=false;sub?.unsubscribe()}},[]);if(state==='loading')return <div className="auth"><div className="panel">Carregando sessão...</div></div>;return state==='none'?<Navigate to="/login" replace/>:<Outlet/>}
function RoleGate({admin}:{admin:boolean}){const[state,setState]=useState<'loading'|'allowed'|'denied'|'error'>('loading');useEffect(()=>{let active=true;async function load(){try{if(!supabase){if(active)setState('error');return}const{data:{user}}=await supabase.auth.getUser();if(!user){if(active)setState('error');return}const{data:profile,error}=await supabase.from('profiles').select('is_superadmin').eq('id',user.id).maybeSingle();if(error){if(active)setState('error');return}const ok=Boolean(profile?.is_superadmin||user.app_metadata?.role==='superadmin'||user.app_metadata?.is_superadmin===true)===admin;if(active)setState(ok?'allowed':'denied')}catch{if(active)setState('error')}}load();return()=>{active=false}},[admin]);if(state==='loading')return <div className="auth"><div className="panel">Carregando acesso...</div></div>;if(state==='error')return <Navigate to="/login" replace/>;if(state==='denied')return <Navigate to={admin?'/dashboard':'/admin/dashboard'} replace/>;return <Outlet/>}
function Metric({label,value,icon:Icon}:{label:string;value:string|number;icon?:React.ComponentType<any>}){return <div className="card">{Icon&&<Icon size={17}/>}<span>{label}</span><b>{value}</b></div>}
function TenantLayout(){const{activeCompany,companies,setActiveCompany}=useTenant();const navigate=useNavigate();async function logout(){await supabase?.auth.signOut();navigate('/login')}if(!activeCompany)return <div className="content"><section className="panel"><Building2 size={32}/><h2>Nenhuma empresa vinculada</h2><p>Seu usuário ainda não possui um consultório associado.</p></section></div>;const nav=[['/dashboard','Dashboard',LayoutDashboard],['/agenda','Agenda',CalendarDays],['/pacientes','Pacientes',Users],['/profissionais','Profissionais',UserRound],['/whatsapp','WhatsApp',MessageCircle],['/automacoes','Automações',Bot],['/configuracoes','Configurações',Settings]] as const;return <div className="layout"><aside><Logo/><div className="company"><strong>{activeCompany.name}</strong><small>Tenant ativo</small></div>{companies.length>1&&<select className="company-select" value={activeCompany.id} onChange={e=>{const c=companies.find(x=>x.id===e.target.value);if(c)setActiveCompany(c)}}>{companies.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select>}<nav className="sidebar-nav">{nav.map(([path,label,Icon])=><NavLink key={path} to={path}><Icon size={18}/><span>{label}</span></NavLink>)}</nav><div className="profile"><b>U</b><span>Conta<small>Tenant protegido por RLS</small></span><button onClick={logout}><LogOut size={15}/></button></div></aside><main><header><strong>{activeCompany.name}</strong><span>Operação</span></header><Outlet/></main></div>}
function TenantPage({title,description,children,action}:{title:string;description:string;children?:React.ReactNode;action?:React.ReactNode}){return <div className="content"><div className="head"><div><h1>{title}</h1><p>{description}</p></div>{action}</div>{children}</div>}
function AdminLayout(){return <div className="layout"><aside><Logo/><div className="company"><strong>Administração</strong><small>Visão global da plataforma</small></div><nav className="sidebar-nav">{[['/admin/dashboard','Dashboard',LayoutDashboard],['/admin/empresas','Empresas',Building2],['/admin/usuarios','Usuários',Users],['/admin/permissoes','Permissões',ShieldCheck],['/admin/whatsapp','WhatsApp',MessageCircle],['/admin/mensagens','Mensagens',BarChart3],['/admin/automacoes','Automações',Bot],['/admin/atividade','Atividade',Activity],['/admin/configuracoes','Configurações',Settings]].map(([path,label,Icon]:any)=><NavLink key={path} to={path}><Icon size={18}/><span>{label}</span></NavLink>)}</nav></aside><main><header><strong>Consulta Pro</strong><span>Superadmin</span></header><Outlet/></main></div>}
function AdminPlaceholder({title,description,icon:Icon}:{title:string;description:string;icon:any}){return <TenantPage title={title} description={description}><section className="panel placeholder"><Icon size={40}/><h2>{title}</h2><p>A estrutura administrativa está pronta.</p></section></TenantPage>}
function AdminDashboard(){return <TenantPage title="Visão geral" description="Operação global do Consulta Pro."><div className="metrics"><Metric label="Empresas" value="—" icon={Building2}/><Metric label="Pacientes" value="—" icon={Users}/><Metric label="Atendimentos" value="—" icon={CalendarDays}/><Metric label="Mensagens" value="—" icon={MessageCircle}/></div></TenantPage>}
function AdminCompanies(){return <TenantPage title="Empresas" description="Todos os consultórios e clínicas da plataforma."><section className="panel"><div className="empty-box"><Building2 size={35}/><h2>Nenhuma empresa</h2></div></section></TenantPage>}
function AdminUsers(){return <TenantPage title="Usuários" description="Acessos vinculados aos consultórios da plataforma."><section className="panel"><Empty text="Nenhum usuário."/></section></TenantPage>}
function Permissions(){return <TenantPage title="Permissões" description="Perfis padrão do Consulta Pro."><section className="panel"><div className="row"><ShieldCheck size={18}/><span><b>Owner</b><small>Acesso total à empresa</small></span></div></section></TenantPage>}
function Empty({text}:{text:string}){return <div className="empty-box"><p>{text}</p></div>}
function AgendaPage(){return <TenantPage title="Agenda" description="Calendário de atendimentos do consultório."><section className="panel"><h2>Agenda</h2><p>Calendário operacional.</p></section></TenantPage>}
function PatientsPage(){return <TenantPage title="Pacientes" description="Cadastro, contato e histórico dos pacientes."><section className="panel"><h2>Pacientes</h2><p>Cadastros do consultório.</p></section></TenantPage>}
function ProfessionalsPage(){return <TenantPage title="Profissionais" description="Equipe, especialidades e agenda de atendimento."><section className="panel"><h2>Profissionais</h2><p>Equipe do consultório.</p></section></TenantPage>}
function WhatsAppPage(){return <TenantPage title="WhatsApp" description="Conecte o WhatsApp do consultório e gerencie mensagens padrão."><section className="panel"><h2>WhatsApp</h2><p>Integração preparada.</p></section></TenantPage>}
function AutomationsPage(){return <TenantPage title="Automações" description="Regras de lembretes e mensagens automáticas."><section className="panel"><h2>Automações</h2><p>Regras de automação.</p></section></TenantPage>}
function TenantSettingsPage(){return <TenantPage title="Configurações" description="Dados do consultório e da conta."><section className="panel"><h2>Configurações</h2><p>Configurações do consultório.</p></section></TenantPage>}
function App(){return <Routes><Route path="/" element={<Landing/>}/><Route path="/login" element={<Login/>}/><Route element={<SessionGate/>}><Route element={<TenantProvider><Outlet/></TenantProvider>}><Route element={<RoleGate admin={true}/>}><Route element={<AdminLayout/>}><Route path="admin/dashboard" element={<AdminDashboard/>}/><Route path="admin/empresas" element={<AdminCompanies/>}/><Route path="admin/usuarios" element={<AdminUsers/>}/><Route path="admin/permissoes" element={<Permissions/>}/><Route path="admin/whatsapp" element={<AdminPlaceholder title="WhatsApp" description="Saúde das integrações por empresa." icon={MessageCircle}/>}/><Route path="admin/mensagens" element={<AdminPlaceholder title="Mensagens" description="Mensageria global da plataforma." icon={BarChart3}/>}/><Route path="admin/automacoes" element={<AdminPlaceholder title="Automações" description="Automações executadas pelos tenants." icon={Bot}/>}/><Route path="admin/atividade" element={<AdminPlaceholder title="Atividade" description="Auditoria e eventos da plataforma." icon={Activity}/>}/><Route path="admin/configuracoes" element={<AdminPlaceholder title="Configurações" description="Configurações globais da plataforma." icon={Settings}/>}/></Route></Route><Route element={<RoleGate admin={false}/>}><Route element={<TenantLayout/>}><Route path="dashboard" element={<TenantPage title="Dashboard" description="Visão geral do consultório."><section className="panel"><h2>Dashboard</h2><p>Visão geral.</p></section></TenantPage>}/><Route path="agenda" element={<AgendaPage/>}/><Route path="pacientes" element={<PatientsPage/>}/><Route path="profissionais" element={<ProfessionalsPage/>}/><Route path="whatsapp" element={<WhatsAppPage/>}/><Route path="automacoes" element={<AutomationsPage/>}/><Route path="configuracoes" element={<TenantSettingsPage/>}/></Route></Route></Route><Route path="/app" element={<Navigate to="/dashboard" replace/>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes>}
