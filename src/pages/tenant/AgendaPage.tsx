import React, { useEffect, useMemo, useState } from 'react'
import { ArrowLeft, CalendarDays, ChevronRight, Plus, Search, XCircle } from 'lucide-react'
import { supabase } from '../../lib/supabase'
import { useTenant } from '../../contexts/TenantContext'
import { Empty, TenantPage, getSupabase, localDateKey, localDateTimeToUtc, utcToLocalInput, formatZoned, BR_TIMEZONE, zonedParts } from '../shared'
function AgendaPage(){
  const{activeCompany}=useTenant();
  const[today,setToday]=useState(()=>new Date()); const[view,setView]=useState<'day'|'week'|'month'|'list'>('month');
  const[items,setItems]=useState<any[]>([]);const[patients,setPatients]=useState<any[]>([]);const[pros,setPros]=useState<any[]>([]);
  const[open,setOpen]=useState(false);const[editing,setEditing]=useState<any>(null);
  const[patient,setPatient]=useState('');const[professional,setProfessional]=useState('');const[date,setDate]=useState('');const[time,setTime]=useState('');const[duration,setDuration]=useState('60');const[type,setType]=useState('Consulta');const[notes,setNotes]=useState('');const[status,setStatus]=useState('scheduled');const[error,setError]=useState('');const[loading,setLoading]=useState(true);
  const[search,setSearch]=useState('');const[filterProfessional,setFilterProfessional]=useState('');const[filterStatus,setFilterStatus]=useState('');const[datePickerOpen,setDatePickerOpen]=useState(false);const[pickerMonth,setPickerMonth]=useState(()=>new Date());
  const tz=activeCompany?.timezone||BR_TIMEZONE;const dayKey=localDateKey(today,tz);
  const range=useMemo(()=>{if(view==='day'||view==='list')return{start:dayKey,end:dayKey};if(view==='week'){const d=new Date(today);const w=d.getDay()||7;d.setDate(d.getDate()-w+1);const e=new Date(d);e.setDate(e.getDate()+6);return{start:localDateKey(d,tz),end:localDateKey(e,tz)}}const first=new Date(today.getFullYear(),today.getMonth(),1);const last=new Date(today.getFullYear(),today.getMonth()+1,0);return{start:localDateKey(first,tz),end:localDateKey(last,tz)}},[today,dayKey,tz,view]);
  async function load(){if(!supabase||!activeCompany)return;setLoading(true);setError('');const from=localDateTimeToUtc(range.start+'T00:00',tz);const endDate=new Date(range.end+'T12:00:00');endDate.setDate(endDate.getDate()+1);const to=localDateTimeToUtc(localDateKey(endDate,tz)+'T00:00',tz);const[a,b,c]=await Promise.all([supabase.from('appointments').select('*,patients(full_name),professionals(name)').eq('company_id',activeCompany.id).gte('starts_at',from.toISOString()).lt('starts_at',to.toISOString()).order('starts_at'),supabase.from('patients').select('id,full_name').eq('company_id',activeCompany.id).eq('status','active').order('full_name'),supabase.from('professionals').select('id,name').eq('company_id',activeCompany.id).eq('status','active').order('name')]);if(a.error)setError(a.error.message);else setItems(a.data||[]);if(b.error)setError(b.error.message);else setPatients(b.data||[]);if(c.error)setError(c.error.message);else setPros(c.data||[]);setLoading(false)}
  useEffect(()=>{let cancelled=false;async function init(){if(!supabase||!activeCompany)return;const{data,error}=await supabase.rpc('server_now');if(!cancelled&&!error&&data){const k=localDateKey(new Date(data),tz);const d=new Date(k+'T12:00:00');setToday(d);setPickerMonth(d)}}init();return()=>{cancelled=true}},[activeCompany?.id,tz]);
  useEffect(()=>{load()},[activeCompany?.id,tz,range.start,range.end]);
  const filteredItems=useMemo(()=>{const q=search.trim().toLowerCase();return items.filter(a=>{const name=String(a.patients?.full_name||'').toLowerCase();const pro=String(a.professionals?.name||'').toLowerCase();return(!q||name.includes(q)||pro.includes(q))&&(!filterProfessional||a.professional_id===filterProfessional)&&(!filterStatus||a.status===filterStatus)})},[items,search,filterProfessional,filterStatus]);
  function clearFilters(){setSearch('');setFilterProfessional('');setFilterStatus('')}
  function resetForm(slot?:number){setEditing(null);setDate(dayKey);setTime(slot===undefined?'09:00':String(slot).padStart(2,'0')+':00');setPatient('');setProfessional('');setDuration('60');setType('Consulta');setNotes('');setStatus('scheduled');setError('');setOpen(true)}
  function editAppointment(a:any){setEditing(a);const local=utcToLocalInput(a.starts_at,activeCompany?.timezone||BR_TIMEZONE);setDate(local.date);setTime(local.time);setPatient(a.patient_id);setProfessional(a.professional_id);setDuration(String(Math.max(15,Math.round((new Date(a.ends_at).getTime()-new Date(a.starts_at).getTime())/60000))));setType(a.appointment_type||'Consulta');setNotes(a.notes||'');setStatus(a.status||'scheduled');setError('');setOpen(true)}
  async function save(e:React.FormEvent){e.preventDefault();if(!supabase||!activeCompany)return;setError('');if(!patient||!professional){setError('Selecione paciente e profissional.');return}if(!date||!time){setError('Informe data e horário.');return}const minutes=Number(duration);if(!Number.isFinite(minutes)||minutes<15||minutes>480){setError('A duração deve ficar entre 15 minutos e 8 horas.');return}const start=localDateTimeToUtc(date+'T'+time,activeCompany.timezone||BR_TIMEZONE);if(Number.isNaN(start.getTime())){setError('Data ou horário inválido.');return}const end=new Date(start.getTime()+minutes*60000);const now=new Date();if(!editing&&start.getTime()<now.getTime()-60000){setError('Não é possível criar atendimento em data e horário passados.');return}if(end.getUTCDate()!==start.getUTCDate()&&end.getTime()-start.getTime()<24*60*60*1000){setError('O atendimento não pode ultrapassar a virada do dia.');return}const q=supabase.from('appointments').select('id').eq('company_id',activeCompany.id).eq('professional_id',professional).neq('status','cancelled').lt('starts_at',end.toISOString()).gt('ends_at',start.toISOString());const{data:conflict,error:conflictError}=editing?await q.neq('id',editing.id).limit(1):await q.limit(1);if(conflictError){setError('Não foi possível validar o horário. Tente novamente.');return}if(conflict?.length){setError('Este profissional já possui atendimento neste horário. Escolha outro horário.');return}const payload={patient_id:patient,professional_id:professional,starts_at:start.toISOString(),ends_at:end.toISOString(),appointment_type:type,notes:notes.trim()||null,status};const result=editing?await supabase.from('appointments').update(payload).eq('id',editing.id).eq('company_id',activeCompany.id):await supabase.from('appointments').insert({company_id:activeCompany.id,...payload});if(result.error)setError('Não foi possível salvar o atendimento. Tente novamente.');else{setOpen(false);await load()}}
  async function changeStatus(id:string,nextStatus:string){if(!supabase||!activeCompany)return;const{error}=await supabase.from('appointments').update({status:nextStatus}).eq('id',id).eq('company_id',activeCompany.id);if(error)setError(error.message);else await load()} async function remove(id:string){await changeStatus(id,'cancelled')}
  function shiftView(delta:number){setToday(d=>{const n=new Date(d);if(view==='day'||view==='list')n.setDate(n.getDate()+delta);else if(view==='week')n.setDate(n.getDate()+delta*7);else n.setMonth(n.getMonth()+delta);return n})}
  const viewTitle=view==='month'?today.toLocaleDateString('pt-BR',{month:'long',year:'numeric'}):view==='week'?(()=>{const d=new Date(today);const w=d.getDay()||7;d.setDate(d.getDate()-w+1);const e=new Date(d);e.setDate(e.getDate()+6);return d.toLocaleDateString('pt-BR',{day:'2-digit',month:'short'})+' — '+e.toLocaleDateString('pt-BR',{day:'2-digit',month:'short',year:'numeric'})})():today.toLocaleDateString('pt-BR',{weekday:'long',day:'2-digit',month:'long',year:'numeric'});
  const statusNames:any={scheduled:'Agendado',confirmed:'Confirmado',completed:'Realizado',cancelled:'Cancelado'};
  const appointmentCard=(a:any,compact=false)=><div className={compact?'appointment-card compact':'appointment-card'} key={a.id} onClick={()=>editAppointment(a)}><div><b>{formatZoned(a.starts_at,tz,{hour:'2-digit',minute:'2-digit'})} · {a.patients?.full_name||'Paciente'}</b><small>{a.professionals?.name||'Profissional'} · {a.appointment_type||'Consulta'} · {statusNames[a.status]||a.status}</small></div>{!compact&&<span><button type="button" className="back-link" onClick={e=>{e.stopPropagation();editAppointment(a)}}>Editar</button>{a.status!=='confirmed'&&a.status!=='completed'&&<button type="button" className="back-link" onClick={e=>{e.stopPropagation();changeStatus(a.id,'confirmed')}}>Confirmar</button>}{a.status==='confirmed'&&<button type="button" className="back-link" onClick={e=>{e.stopPropagation();changeStatus(a.id,'completed')}}>Concluir</button>}{a.status!=='cancelled'&&a.status!=='completed'&&<button type="button" className="back-link danger" onClick={e=>{e.stopPropagation();remove(a.id)}}>Cancelar</button>}</span>}</div>;
  const renderDay=(key:string,compact=false)=>{const dayItems=filteredItems.filter(a=>localDateKey(new Date(a.starts_at),tz)===key).sort((a,b)=>new Date(a.starts_at).getTime()-new Date(b.starts_at).getTime());return <div className={compact?'calendar-day compact':'calendar-day'}>{dayItems.map(a=>appointmentCard(a,compact))}</div>};
  const weekKeys=useMemo(()=>{const d=new Date(today);const w=d.getDay()||7;d.setDate(d.getDate()-w+1);return Array.from({length:7},(_,i)=>{const x=new Date(d);x.setDate(x.getDate()+i);return localDateKey(x,tz)})},[today,tz]);
  const monthKeys=useMemo(()=>{const first=new Date(today.getFullYear(),today.getMonth(),1);const start=new Date(first);const w=start.getDay()||7;start.setDate(start.getDate()-w+1);return Array.from({length:42},(_,i)=>{const x=new Date(start);x.setDate(x.getDate()+i);return localDateKey(x,tz)})},[today,tz]);
  const holidayMap:Record<string,string>={'2026-01-01':'Confraternização Universal','2026-04-03':'Paixão de Cristo','2026-04-21':'Tiradentes','2026-05-01':'Dia do Trabalho','2026-09-07':'Independência do Brasil','2026-10-12':'Nossa Senhora Aparecida','2026-11-02':'Finados','2026-11-15':'Proclamação da República','2026-11-20':'Dia da Consciência Negra','2026-12-25':'Natal'};
  const pickerKeys=useMemo(()=>{const first=new Date(pickerMonth.getFullYear(),pickerMonth.getMonth(),1);const start=new Date(first);const w=start.getDay()||7;start.setDate(start.getDate()-w+1);return Array.from({length:42},(_,i)=>{const x=new Date(start);x.setDate(x.getDate()+i);return localDateKey(x,tz)})},[pickerMonth,tz]);
  const monthLabel=pickerMonth.toLocaleDateString('pt-BR',{month:'long',year:'numeric'}); const goMonth=(delta:number)=>setPickerMonth(d=>{const n=new Date(d);n.setMonth(n.getMonth()+delta);return n});
  return <TenantPage title="Agenda" description="Calendário de atendimentos do consultório." action={<button className="hero-btn" onClick={()=>resetForm()}><Plus size={16}/> Novo atendimento</button}><section className="panel"><div className="agenda-toolbar"><div className="agenda-nav"><button type="button" className="agenda-today" onClick={()=>setToday(new Date())}>Hoje</button><button type="button" className="agenda-nav-btn" onClick={()=>shiftView(-1)} title="Anterior"><ArrowLeft size={18}/></button><button type="button" className="agenda-nav-btn" onClick={()=>shiftView(1)} title="Próximo"><ChevronRight size={18}/></button><div className="agenda-date-picker-wrap"><button type="button" className="agenda-date-picker" title="Selecionar data" onClick={()=>{setPickerMonth(today);setDatePickerOpen(v=>!v)}}><CalendarDays size={17}/></button>{datePickerOpen&&<div className="agenda-date-popover"><div className="date-popover-head"><button type="button" onClick={()=>goMonth(-1)}><ArrowLeft size={15}/></button><strong>{monthLabel}</strong><button type="button" onClick={()=>goMonth(1)}><ChevronRight size={15}/></button></div><div className="date-popover-weekdays">{['S','T','Q','Q','S','S','D'].map((d,i)=><b key={i}>{d}</b>)}</div><div className="date-popover-grid">{pickerKeys.map(k=>{const muted=k.slice(0,7)!==dayKey.slice(0,7);const holiday=holidayMap[k];return <button type="button" key={k} title={holiday||undefined} className={(k===dayKey?'selected ':'')+(muted?'muted ':'')+(holiday?'holiday':'')} onClick={()=>{setToday(new Date(k+'T12:00:00'));setDatePickerOpen(false)}}>{new Date(k+'T12:00:00').getDate()}{holiday&&<i/>}</button>})}</div></div>}</div></div><div className="agenda-title"><div><h2>{viewTitle}</h2><p>{filteredItems.length} atendimento(s)</p></div><div className="agenda-view-toggle">{(['day','week','month','list'] as const).map(v=><button key={v} type="button" className={view===v?'active':''} onClick={()=>setView(v)}>{v==='day'?'Dia':v==='week'?'Semana':v==='month'?'Mês':'Lista'}</button>)}</div></div><div className="agenda-filters"><div className="search-wrap"><Search size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar paciente ou profissional..."/></div><select value={filterProfessional} onChange={e=>setFilterProfessional(e.target.value)}><option value="">Todos os profissionais</option>{pros.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><select value={filterStatus} onChange={e=>setFilterStatus(e.target.value)}><option value="">Todos os status</option><option value="scheduled">Agendados</option><option value="confirmed">Confirmados</option><option value="completed">Realizados</option><option value="cancelled">Cancelados</option></select>{(search||filterProfessional||filterStatus)&&<button type="button" className="back-link" onClick={clearFilters}>Limpar</button>}</div>{loading?<div className="empty-box">Carregando agenda...</div>:view==='day'||view==='list'?<div>{view==='day'?renderDay(dayKey):filteredItems.map(a=>appointmentCard(a))}</div>:view==='week'?<div className="agenda-week-grid">{weekKeys.map(k=><div key={k}><h3>{new Date(k+'T12:00:00').toLocaleDateString('pt-BR',{weekday:'short',day:'2-digit'})}</h3>{renderDay(k,true)}</div>)}</div>:<div className="agenda-month-grid">{monthKeys.map(k=><div key={k} className={'month-cell '+(k.slice(0,7)!==dayKey.slice(0,7)?'muted':'')+(k===dayKey?' today':'')}><b>{new Date(k+'T12:00:00').getDate()}</b>{holidayMap[k]&&<small title={holidayMap[k]}>Feriado</small>}{filteredItems.filter(a=>localDateKey(new Date(a.starts_at),tz)===k).slice(0,3).map(a=>appointmentCard(a,true))}</div>)}</div>}</div></section>{open&&<div className="modal-backdrop"><form className="modal panel" onSubmit={save}><div className="head"><div><h2>{editing?'Editar atendimento':'Novo atendimento'}</h2><p>Preencha os dados do agendamento.</p></div><button type="button" className="icon-btn" onClick={()=>setOpen(false)}><XCircle size={18}/></button></div><div className="form-grid"><label>Paciente*<select required value={patient} onChange={e=>setPatient(e.target.value)}><option value="">Selecione</option>{patients.map(p=><option key={p.id} value={p.id}>{p.full_name}</option>)}</select></label><label>Profissional*<select required value={professional} onChange={e=>setProfessional(e.target.value)}><option value="">Selecione</option>{pros.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>Data*<input type="date" required value={date} onChange={e=>setDate(e.target.value)}/></label><label>Horário*<input type="time" required value={time} onChange={e=>setTime(e.target.value)}/></label><label>Duração<select value={duration} onChange={e=>setDuration(e.target.value)}><option value="15">15 min</option><option value="30">30 min</option><option value="45">45 min</option><option value="60">1 hora</option><option value="90">1h30</option><option value="120">2 horas</option></select></label><label>Tipo<input value={type} onChange={e=>setType(e.target.value)}/></label><label>Status<select value={status} onChange={e=>setStatus(e.target.value)}><option value="scheduled">Agendado</option><option value="confirmed">Confirmado</option><option value="completed">Realizado</option><option value="cancelled">Cancelado</option></select></label><label style={{gridColumn:'1 / -1'}}>Observações<textarea rows={4} value={notes} onChange={e=>setNotes(e.target.value)}/></label></div>{error&&<div className="form-error">{error}</div>}<div><button className="hero-btn">{editing?'Salvar alterações':'Criar atendimento'}</button><button type="button" className="back-link" onClick={()=>setOpen(false)}>Cancelar</button></div></form></div>}</TenantPage>
}
export { AgendaPage }
